(ns discourje.examples.experimental.benchmarks
  (require [discourje.examples.experimental.ring.clojure]))

(defn -main
  [& args]
  ;(require [discourje.examples.experimental.chess.clojure]))

  (load "ring/clojure")
  (load "ring/clojure"))

(-main)